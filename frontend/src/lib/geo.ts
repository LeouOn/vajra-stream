/**
 * Default geographic coordinates (San Francisco).
 *
 * Used as a fallback when no user location is available — e.g. when fetching
 * astrology data before geolocation resolves. Mirrors
 * `config/settings.py` `DEFAULT_LATITUDE` / `DEFAULT_LONGITUDE`; keep both
 * copies in sync.
 */
export const DEFAULT_LAT = 37.7749;
export const DEFAULT_LNG = -122.4194;

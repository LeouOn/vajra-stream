// frontend/src/types/radionics.ts
// Shared types for radionics components.

/** The 5 dial values used in the radionics crystal bowl broadcast. */
export interface Dimensions {
  d1: number;
  d2: number;
  d3: number;
  d4: number;
  d5: number;
}
